const leg = (name) => {
  const value = {
    name: "20",
    otherValue: "20",
  };
};

const name = "name";

const sum = () => {
  const value =[10, 20, 30, 40] 
}

class Foo {
  geName = function bar() {
    let obj  = {}
    
  }
}


"suelto"

for (const iterator of object) {
 console.log("get") 
}
